[project]
name = "grammar-check"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.102.0",
    "claude-agent-sdk>=0.2.82",
    "openai>=2.37.0",
]
